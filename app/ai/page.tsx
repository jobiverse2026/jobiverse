import { Bot, BrainCircuit, ShieldCheck, UserCheck } from "lucide-react";
import { adminSupabase } from "@/lib/supabase/admin";
export default async function AITrustPage(){const{data:features}=await adminSupabase.from("ai_feature_registry").select("feature_key,name,description,audience,status,human_review_required,disclaimer").in("status",["coming_soon","limited_beta","live"]).order("name");return <main className="min-h-screen bg-[#f5f5f3] px-5 pb-24 pt-36 sm:px-8"><div className="mx-auto max-w-7xl"><section className="rounded-[2.75rem] bg-zinc-950 p-8 text-white sm:p-12"><BrainCircuit/><p className="mt-5 text-xs font-bold uppercase tracking-[.2em] text-zinc-500">Responsible AI at JobiVerse</p><h1 className="mt-3 text-4xl font-semibold sm:text-6xl">Intelligence with boundaries.</h1><p className="mt-4 max-w-3xl text-zinc-400">JobiVerse AI will assist people-it will not promise jobs, invent credentials or make final hiring decisions. Features remain unavailable until security, quality and commercial readiness are approved.</p></section><section className="mt-7 grid gap-4 md:grid-cols-3"><Principle icon={<UserCheck/>} title="Human decisions" text="Employers and recruiters remain responsible for fair, lawful and job-relevant hiring decisions."/><Principle icon={<ShieldCheck/>} title="Clear disclosures" text="Scores and suggestions are guidance, not guarantees or hidden automated decisions."/><Principle icon={<Bot/>} title="Controlled launch" text="Every feature moves through coming soon, internal testing, limited beta and live stages."/></section><section className="mt-7 grid gap-5 lg:grid-cols-2">{features?.map(feature=><article key={feature.feature_key} className="rounded-[2rem] border border-zinc-200 bg-white p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-zinc-400">{feature.audience.join(" | ")}</p><h2 className="mt-2 text-2xl font-bold">{feature.name}</h2></div><span className={`rounded-full px-3 py-1 text-[10px] font-bold uppercase ${feature.status==="live"?"bg-emerald-100 text-emerald-700":feature.status==="limited_beta"?"bg-amber-100 text-amber-700":"bg-zinc-100 text-zinc-600"}`}>{feature.status.replaceAll("_"," ")}</span></div><p className="mt-4 text-sm leading-6 text-zinc-500">{feature.description}</p><p className="mt-5 rounded-xl bg-zinc-50 p-4 text-xs leading-5 text-zinc-600">{feature.disclaimer}</p>{feature.human_review_required&&<p className="mt-3 flex items-center gap-2 text-xs font-semibold text-emerald-700"><UserCheck size={14}/>Human review required</p>}</article>)}</section></div></main>}
function Principle({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <article className="rounded-3xl border border-zinc-200 bg-white p-7">{icon}<h2 className="mt-5 text-xl font-bold">{title}</h2><p className="mt-3 text-sm leading-6 text-zinc-500">{text}</p></article>}
