import type { Metadata } from "next";
import { Boxes, Braces, ShieldCheck, Sparkles } from "lucide-react";
import { CustomPortalRequestForm } from "@/components/employer/custom-portal/CustomPortalRequestForm";
import { customPortalIndustries } from "@/lib/custom-portals/catalog";

export const metadata:Metadata={title:"Custom Organisation Portals | JobiVerse",description:"Request a secure custom portal designed around your organisation's real workflows."};
export default function CustomPortalPage(){return <main className="min-h-screen bg-[#f8f6fa] pb-24 pt-32 text-zinc-950"><section className="px-5 sm:px-8"><div className="mx-auto max-w-[1400px]"><div className="jv-brand-gradient relative overflow-hidden rounded-[3rem] p-8 text-white sm:p-14"><div className="absolute -right-28 -top-28 h-80 w-80 rounded-full border border-white/10"/><p className="text-xs font-black uppercase tracking-[.2em] text-violet-200">Beyond recruitment</p><h1 className="mt-5 max-w-5xl text-5xl font-semibold leading-[.95] tracking-[-.055em] sm:text-7xl">Your organisation.<br/>Your workflows. Your portal.</h1><p className="mt-7 max-w-3xl text-lg leading-8 text-zinc-300">Not a consultancy or recruitment agency? Tell us what your team actually needs. JobiVerse can scope and build a focused portal around your operations—not force every industry into the same dashboard.</p><div className="mt-8 grid gap-3 sm:grid-cols-3"><Mini icon={<Boxes/>} title="Pick starting modules"/><Mini icon={<Braces/>} title="Add your workflows"/><Mini icon={<ShieldCheck/>} title="Scope security & access"/></div></div><section className="mt-10 rounded-[2.75rem] border border-violet-100 bg-white p-7 sm:p-10"><p className="text-xs font-black uppercase tracking-[.2em] text-violet-700">Transparent starting price</p><div className="mt-3 grid gap-7 lg:grid-cols-[1fr_auto] lg:items-end"><div><h2 className="text-4xl font-semibold tracking-[-.045em]">Simple pricing: ₹500 per requested module + ₹4,999/year.</h2><p className="mt-4 max-w-3xl leading-7 text-zinc-600">No default modules are forced into the portal. The client chooses only the modules they need; setup is calculated from that final module count. The annual fee covers platform access, hosting, standard updates and support.</p></div><div className="rounded-2xl bg-violet-50 p-5 text-sm leading-6 text-violet-950"><strong>One plan. No confusing tiers.</strong><br/>The request form shows the complete first-year price live. Only integrations or data migration receive a separate written quote.</div></div></section><div className="mt-14 grid gap-10 lg:grid-cols-[.82fr_1.18fr] lg:items-start"><div className="lg:sticky lg:top-28"><p className="text-xs font-black uppercase tracking-[.2em] text-violet-700">Module library</p><h2 className="mt-3 text-4xl font-semibold tracking-[-.045em]">Ideas for every kind of organisation.</h2><p className="mt-4 leading-7 text-zinc-600">Use these as a starting point. Select only what matters and describe any workflow that is unique to you.</p><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-1">{customPortalIndustries.map(item=><div key={item.slug} className="rounded-2xl border border-violet-100 bg-white p-4"><p className="font-bold">{item.label}</p><p className="mt-1 text-xs leading-5 text-zinc-500">{item.modules.slice(0,3).join(" · ")}</p></div>)}</div></div><CustomPortalRequestForm/></div></div></section></main>}
function Mini({icon,title}:{icon:React.ReactNode;title:string}){return <div className="flex items-center gap-3 rounded-2xl border border-white/15 bg-white/[.08] p-4"><span className="text-violet-200">{icon}</span><span className="text-sm font-bold">{title}</span></div>}
