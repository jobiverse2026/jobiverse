import{BadgeCheck,Check,ShieldCheck,Sparkles}from"lucide-react";import{redirect}from"next/navigation";import{createServerSupabaseClient}from"@/lib/supabase/server";import{requestPlan,cancelPlan}from"./actions";
export default async function PlansPage(){const supabase=await createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login/candidate?next=/plans");const{data:person}=await supabase.from("users").select("role").eq("id",user.id).single();if(!["candidate","employer"].includes(person?.role??""))redirect("/dashboard");const[{data:plans},{data:subscriptions}]=await Promise.all([supabase.from("platform_plans").select("*").eq("audience",person!.role).eq("is_active",true).order("sort_order"),supabase.from("platform_subscriptions").select("id,plan_id,status,starts_at,ends_at,created_at,platform_plans(name)").eq("user_id",user.id).order("created_at",{ascending:false})]);return <main className="min-h-screen bg-[#f5f5f3] px-5 pb-24 pt-36 sm:px-8"><div className="mx-auto max-w-7xl"><section className="rounded-[2.75rem] bg-zinc-950 p-8 text-white sm:p-12"><Sparkles/><p className="mt-5 text-xs font-bold uppercase tracking-[.2em] text-zinc-500">{person!.role} plans</p><h1 className="mt-3 text-4xl font-semibold sm:text-6xl">Choose support, not restrictions.</h1><p className="mt-4 max-w-3xl text-zinc-400">{person!.role==="candidate"?"Job discovery and applications remain free. Paid plans cover optional guidance and acceleration.":"Start free, then add operational support when your hiring volume requires it."}</p></section><section className="mt-7 grid gap-5 lg:grid-cols-3">{plans?.map(plan=><article key={plan.id} className={`rounded-[2rem] border bg-white p-7 ${plan.slug.includes("growth")||plan.slug.includes("plus")?"border-zinc-950 shadow-xl":"border-zinc-200"}`}><BadgeCheck/><h2 className="mt-5 text-2xl font-bold">{plan.name}</h2><p className="mt-3 min-h-16 text-sm leading-6 text-zinc-500">{plan.description}</p><p className="mt-6 text-4xl font-bold">{plan.billing_interval==="custom"?"Let's talk":plan.price?`₹${Number(plan.price).toLocaleString("en-IN")}`:"Free"}</p><p className="mt-1 text-xs uppercase text-zinc-400">{plan.billing_interval}</p><div className="mt-6 space-y-3">{(plan.features as string[]).map(feature=><p key={feature} className="flex gap-2 text-sm"><Check className="shrink-0 text-emerald-600" size={17}/>{feature}</p>)}</div><form action={requestPlan} className="mt-7"><input type="hidden" name="planId" value={plan.id}/><button className="w-full cursor-pointer rounded-xl bg-zinc-950 px-5 py-3 font-semibold text-white">{plan.price>0?"Request activation":plan.billing_interval==="custom"?"Request proposal":"Activate free plan"}</button></form></article>)}</section><section className="mt-7 rounded-[2rem] border border-zinc-200 bg-white p-7"><div className="flex items-center gap-3"><ShieldCheck/><h2 className="text-2xl font-bold">Plan history</h2></div><div className="mt-5 space-y-3">{subscriptions?.length?subscriptions.map(item=>{const plan=Array.isArray(item.platform_plans)?item.platform_plans[0]:item.platform_plans;return <div key={item.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-zinc-50 p-4"><div><p className="font-semibold">{plan?.name??"Plan"}</p><p className="mt-1 text-xs text-zinc-500">Requested {new Date(item.created_at).toLocaleDateString("en-IN")}</p></div><div className="flex items-center gap-3"><span className="rounded-full bg-white px-3 py-1 text-xs font-bold uppercase">{item.status.replaceAll("_"," ")}</span>{["requested","active"].includes(item.status)&&<form action={cancelPlan}><input type="hidden" name="subscriptionId" value={item.id}/><button className="cursor-pointer text-xs font-semibold text-red-600">Cancel</button></form>}</div></div>}):<p className="rounded-2xl border border-dashed p-10 text-center text-sm text-zinc-500">No plan history yet.</p>}</div></section></div></main>}
