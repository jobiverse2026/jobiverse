"use server";import{revalidatePath}from"next/cache";import{redirect}from"next/navigation";import{z}from"zod";import{requireRole}from"@/lib/auth/authorization";
export async function requestPlan(formData:FormData){const planId=z.string().uuid().parse(formData.get("planId"));const{user,supabase,profile}=await requireRole(["candidate","employer"]);const{data:plan,error:readError}=await supabase.from("platform_plans").select("id,audience,price,billing_interval").eq("id",planId).eq("is_active",true).single();if(readError||!plan)throw new Error("This plan is unavailable.");if(plan.audience!==profile.role)throw new Error("This plan is not available for your account type.");const{error}=await supabase.from("platform_subscriptions").insert({user_id:user.id,plan_id:plan.id,status:"requested"});if(error?.code==="23505")throw new Error("You already have an open request or active subscription for this plan.");if(error)throw new Error(error.message);revalidatePath("/plans");redirect("/plans?success=plan_requested")}
export async function cancelPlan(formData:FormData){const id=z.string().uuid().parse(formData.get("subscriptionId"));const{user,supabase}=await requireRole(["candidate","employer"]);const{error}=await supabase.from("platform_subscriptions").update({status:"cancelled",cancelled_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("id",id).eq("user_id",user.id);if(error)throw new Error(error.message);revalidatePath("/plans");redirect("/plans?success=plan_cancelled")}
