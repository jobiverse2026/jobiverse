import { adminSupabase } from "@/lib/supabase/admin";
import { verifyRazorpayWebhook } from "@/lib/payments/razorpay";
export async function POST(request:Request){const body=await request.text();const signature=request.headers.get("x-razorpay-signature")??"";if(!verifyRazorpayWebhook(body,signature))return new Response("Invalid signature",{status:401});const event=JSON.parse(body);const payment=event?.payload?.payment?.entity;if(["payment.captured","order.paid"].includes(event?.event)&&payment?.order_id){const{data:attempt}=await adminSupabase.from("payment_attempts").select("id,user_id,target_type,target_id,local_order_id,amount").eq("gateway_order_id",payment.order_id).maybeSingle();if(attempt){await adminSupabase.from("payment_attempts").update({status:"captured",gateway_payment_id:payment.id}).eq("id",attempt.id);if(attempt.local_order_id)await adminSupabase.from("marketplace_orders").update({status:"paid",paid_at:new Date().toISOString(),payment_reference:payment.id}).eq("id",attempt.local_order_id).eq("status","pending_payment");if(attempt.target_type==="resume_download")await adminSupabase.from("resume_purchases").upsert({user_id:attempt.user_id,template_id:attempt.target_id,payment_attempt_id:attempt.id,amount:attempt.amount},{onConflict:"user_id,template_id",ignoreDuplicates:true});}}const refund=event?.payload?.refund?.entity;if(refund?.id&&["refund.processed","refund.failed"].includes(event?.event)){const status=event.event==="refund.processed"?"processed":"failed";const{data:requestRow}=await adminSupabase.from("marketplace_refund_requests").update({status,gateway_status:refund.status,processed_at:new Date().toISOString()}).eq("gateway_refund_id",refund.id).select("order_id,payment_attempt_id").maybeSingle();if(requestRow&&status==="processed"){await adminSupabase.from("marketplace_orders").update({status:"refunded",payout_status:"held"}).eq("id",requestRow.order_id);await adminSupabase.from("payment_attempts").update({status:"refunded"}).eq("id",requestRow.payment_attempt_id);}}return Response.json({ok:true});}
