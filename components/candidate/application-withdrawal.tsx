"use client";
import { useState } from "react";
import { withdrawApplication } from "@/app/candidates/applications/actions";
export function ApplicationWithdrawal({applicationId}:{applicationId:string}){const[open,setOpen]=useState(false);return <div className="mt-3">{open?<form action={withdrawApplication} className="rounded-2xl border border-red-100 bg-red-50 p-4"><input type="hidden" name="applicationId" value={applicationId}/><p className="text-xs font-bold uppercase text-red-700">Withdraw application</p><select name="reason" required className="mt-3 h-11 w-full rounded-xl border border-red-200 bg-white px-3 text-sm"><option value="">Select reason</option><option value="accepted_other_offer">Accepted another offer</option><option value="role_mismatch">Role mismatch</option><option value="location">Location or work mode</option><option value="compensation">Compensation</option><option value="timing">Timing or availability</option><option value="personal">Personal reasons</option><option value="other">Other</option></select><textarea name="note" maxLength={500} rows={2} placeholder="Optional private note" className="mt-2 w-full rounded-xl border border-red-200 bg-white p-3 text-sm"/><div className="mt-2 flex gap-2"><button className="cursor-pointer rounded-xl bg-red-700 px-4 py-2 text-xs font-bold text-white">Confirm withdrawal</button><button type="button" onClick={()=>setOpen(false)} className="cursor-pointer rounded-xl bg-white px-4 py-2 text-xs font-bold">Cancel</button></div></form>:<button type="button" onClick={()=>setOpen(true)} className="cursor-pointer text-xs font-semibold text-red-600 underline">Withdraw application</button>}</div>}
