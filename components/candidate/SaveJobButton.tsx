"use client";
import { Bookmark } from "lucide-react";
import { useState, useTransition } from "react";
import { toggleSavedJob } from "@/actions/saved-jobs";
export function SaveJobButton({requirementId,saved}:{requirementId:string;saved:boolean}){const[current,setCurrent]=useState(saved);const[message,setMessage]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);const[pending,startTransition]=useTransition();function toggle(){const next=!current;setCurrent(next);setError(null);setMessage(null);startTransition(async()=>{try{await toggleSavedJob(requirementId,next);setMessage(next?"Job saved successfully.":"Job removed from saved opportunities.");}catch(cause){setCurrent(!next);setError(cause instanceof Error?cause.message:"Unable to update saved jobs.");}})}return <div><button type="button" onClick={toggle} disabled={pending} className={`inline-flex w-full cursor-pointer items-center justify-center gap-2 rounded-xl border px-4 py-3 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50 ${current?"border-zinc-950 bg-zinc-950 text-white":"border-zinc-200 bg-white text-zinc-700 hover:border-zinc-400"}`}><Bookmark size={16} fill={current?"currentColor":"none"}/>{pending?"Saving...":current?"Saved":"Save job"}</button>{message&&<p role="status" className="mt-3 rounded-xl bg-emerald-50 p-3 text-xs font-semibold text-emerald-700">{message}</p>}{error&&<p role="alert" className="mt-3 rounded-xl bg-red-50 p-3 text-xs font-semibold text-red-700">{error}</p>}</div>}
