"use client";

import { useActionState } from "react";
import { Flag, LoaderCircle } from "lucide-react";
import { reportMarketplaceService, type ReportServiceState } from "@/app/marketplace/services/[slug]/report-actions";

const initialState:ReportServiceState={};
export function ReportServiceForm({serviceId}:{serviceId:string}){const[state,action,pending]=useActionState(reportMarketplaceService,initialState);return <details className="mt-3 rounded-2xl border border-zinc-200 bg-zinc-50"><summary className="flex cursor-pointer list-none items-center justify-center gap-2 px-5 py-3 text-xs font-semibold text-zinc-500"><Flag size={14}/>Report listing</summary><form action={action} className="space-y-3 border-t border-zinc-200 p-4"><input type="hidden" name="serviceId" value={serviceId}/><select name="reason" required defaultValue="" className="h-11 w-full rounded-xl border border-zinc-200 bg-white px-3 text-sm"><option value="" disabled>Select concern</option><option value="misleading">Misleading information</option><option value="duplicate">Duplicate listing</option><option value="prohibited">Prohibited service</option><option value="copyright">Copyright concern</option><option value="pricing">Pricing concern</option><option value="other">Other</option></select><textarea name="details" required minLength={10} maxLength={1000} placeholder="Explain the concern clearly" className="min-h-24 w-full rounded-xl border border-zinc-200 bg-white p-3 text-sm"/>{state.error&&<p className="text-xs font-semibold text-red-600">{state.error}</p>}{state.success&&<p className="text-xs font-semibold text-emerald-700">{state.success}</p>}<button disabled={pending} className="inline-flex w-full cursor-pointer items-center justify-center gap-2 rounded-xl bg-zinc-950 px-4 py-3 text-sm font-semibold text-white disabled:opacity-60">{pending?<LoaderCircle className="animate-spin" size={15}/>:<Flag size={15}/>}Submit report</button></form></details>}
