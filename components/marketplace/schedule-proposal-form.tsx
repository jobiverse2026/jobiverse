"use client";
import {useActionState} from "react";
import {proposeOrderScheduleWithState,type ScheduleProposalState} from "@/app/marketplace/orders/[id]/actions";

const initialState:ScheduleProposalState={};
export function ScheduleProposalForm({orderId,rescheduleCount}:{orderId:string;rescheduleCount:number}){const[state,action,pending]=useActionState(proposeOrderScheduleWithState,initialState);const today=new Date().toLocaleDateString("en-CA",{timeZone:"Asia/Kolkata"});return <form action={action} className="mt-4 grid gap-3 sm:grid-cols-[1fr_auto]"><input type="hidden" name="orderId" value={orderId}/><input type="date" name="scheduleDate" required min={today} className="h-12 rounded-xl border border-violet-200 bg-white px-4 text-sm"/><div className="grid grid-cols-3 gap-2"><select name="scheduleHour" required defaultValue="10" aria-label="Hour" className="h-12 rounded-xl border border-violet-200 bg-white px-3 text-sm">{Array.from({length:12},(_,index)=>index+1).map(hour=><option key={hour} value={hour}>{String(hour).padStart(2,"0")}</option>)}</select><select name="scheduleMinute" required defaultValue="00" aria-label="Minute" className="h-12 rounded-xl border border-violet-200 bg-white px-3 text-sm">{["00","15","30","45"].map(minute=><option key={minute} value={minute}>{minute}</option>)}</select><select name="scheduleMeridiem" required defaultValue="AM" aria-label="AM or PM" className="h-12 rounded-xl border border-violet-200 bg-white px-3 text-sm"><option value="AM">AM</option><option value="PM">PM</option></select></div><input name="scheduleNote" maxLength={500} placeholder="Optional meeting or availability note" className="h-12 rounded-xl border border-violet-200 bg-white px-4 text-sm sm:col-span-2"/>{state.error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700 sm:col-span-2">{state.error}</p>}{state.success&&<p role="status" className="rounded-xl bg-emerald-50 p-3 text-sm font-semibold text-emerald-700 sm:col-span-2">{state.success}</p>}<button disabled={pending} className="cursor-pointer rounded-xl bg-zinc-950 px-5 py-3 text-sm font-semibold text-white disabled:opacity-60 sm:col-span-2">{pending?"Sending proposal...":"Send proposal"}</button><p className="text-xs text-zinc-500 sm:col-span-2">The other participant must confirm. Reschedules used: {rescheduleCount} of 20.</p></form>}
